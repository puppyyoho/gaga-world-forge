{
  "name": "gaga-world-forge",
  "version": "0.1.1",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check index.js && node --check studio-data.js && node --check jsonl.js && node --check compiler.js && node --check streaming.js"
  }
}
